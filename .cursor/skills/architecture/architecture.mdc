---
name: architecture-rules
description: 分层与架构规范（Controller、异常处理、网关）
---

# 分层与架构规范（必须）

## Controller 层规范

- **Controller 纯协调**：Controller 层禁止承载业务逻辑
  - 禁止：计算、规则判断、事务编排、跨领域聚合等业务逻辑
  - 允许：参数校验、调用 Service、返回统一响应

## 异常处理规范

- **统一使用全局异常处理**
- **Controller 层禁止 `try-catch` 包裹业务调用并吞异常/改写异常**
  - 允许的例外：仅用于**资源关闭/非业务可恢复分支**的局部 `try-catch`（必须说明原因）
  - 否则一律上抛交给全局异常处理

## 网关规范

- **前端接口访问尽可能走网关调用**
- 避免直连内部服务造成鉴权/路由/灰度问题
