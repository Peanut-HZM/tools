---
name: rule
description: This is a new rule
---

# Overview

---
type: "always_apply"
---

# 项目开发规则（Cursor 强制约束版）

> 目标：把“原则”变成 **每次编码都可执行/可检查** 的规则。  
> 约束优先级：**本规则 > 仓库既有约定/代码风格 > 一般最佳实践**。若出现冲突或无法满足，必须先说明原因并向用户确认，禁止默默违规。

## 基础约束（每次对话/编码默认生效）

- **语言与环境**：对话语言必须为中文；默认环境为 Mac（/bin/zsh）。
- **终端执行**：执行命令后必须关注输出与退出码；若命令可能长时间运行，优先用可中断/可观测方式（例如增加 `-q`、`--no-transfer-progress`、指定模块构建），避免无效等待。
- **最小修改原则**：只改与需求/问题直接相关的代码；禁止无关重构、格式化全仓库、批量改名等。

## 编码产出规范（必须）

- **注释**：新增/修改的业务类与对外方法必须补齐类级与方法级注释（说明用途、关键入参/返回、边界条件）。
- **导包**：必须使用 `import`；禁止使用全限定类名（FQN）直接引用（例如 `com.xx.A` 写在代码中）。
- **DTO 字段类型**：禁止在 `entity/request/response/dto` 中直接使用 `enum` 作为字段类型；使用 `String/Integer` 等代码值字段 + 注释/字典映射（必要时在转换层做枚举转换）。
- **ID 生成**：新增数据的 `id` 必须使用项目已存在的雪花算法生成器；禁止自行引入 `UUID.randomUUID()` 作为主键策略（除非项目已有明确约定）。

## 分层与架构规范（必须）

- **Controller 纯协调**：Controller 层禁止承载业务逻辑（计算、规则判断、事务编排、跨领域聚合等都算业务逻辑）。
- **异常处理**：统一使用全局异常处理；Controller 层禁止 `try-catch` 包裹业务调用并吞异常/改写异常。
  - 允许的例外：仅用于**资源关闭/非业务可恢复分支**的局部 `try-catch`（必须说明原因），否则一律上抛交给全局异常处理。
- **网关优先**：前端接口访问尽可能走网关调用（避免直连内部服务造成鉴权/路由/灰度问题）。

## 接口设计规范（Controller / Service 必须遵守）

### 接口定义与对象流转

- **入参**：Controller 入参必须使用 `Request`/DTO 封装后传递到 Service（禁止将一堆原始参数散落在方法签名里）。
- **出参**：Service 返回 `Response` 对象，Controller 仅负责返回项目已有的 `Result`（或项目统一返回体）。
- **命名一致**：Service 方法命名必须与 Controller 的接口方法名称保持一致（便于追踪与搜索）。
- **禁止在 Controller 转换对象**：Controller 层禁止做 `entity/domain` 与 `request/response` 的转换；转换应在 Service（或项目既有的转换/Assembler 层）完成。

### 方法签名与参数数量

- **参数不超过 2 个**：Controller/Service 的业务方法参数不允许超过 2 个；超过时必须封装到 `Request`/DTO。
  - 允许的例外：框架必需参数（例如 `HttpServletRequest/HttpServletResponse`、文件上传 `MultipartFile`）不计入业务参数数量，但仍应尽量封装业务字段。

### 路由与注解（强制校验点）

- **禁止 `/api` 前缀**：Controller 层路由禁止添加 `/api` 前缀。
- **Mapping 必须显式指定 value**：所有 `@GetMapping/@PostMapping/@PutMapping/@DeleteMapping/...` 必须明确指定 `value`，禁止空注解。
- **Mapping value 不允许为空/重复**：同一个 Controller（以及同模块范围内）必须保证 `value` 语义清晰且不重复。
- **路径命名**：路径必须语义明确，避免通用词（如 `/get`、`/list`）；层级建议不超过 3 级。
- **禁止 PathVariable**：禁止使用 `/{param}` 形式的路径参数，避免网关路由冲突；路径参数统一使用 `@RequestParam`（或放入 `Request` 的 body）而非 `@PathVariable`。
- **批量操作**：批量接口必须使用专门 `Request` 封装参数，禁止直接接收 `List` 作为入参。
- **更新操作入参**：更新必须使用包含 `ID + 其他业务字段` 的 `Request`，禁止只传一个 `id` 再额外散落其他参数。
- **用户相关接口**：禁止直接传 `userId`；后端必须从 token/上下文获取当前登录用户信息。

## 环境与配置（必须）

- **多环境配置文件**：必须为 `local/dev/prod` 等环境建立独立配置文件，并通过启动参数选择。
- **端口约束**：启动服务时禁止擅自修改端口号；以配置文件为准（除非用户明确要求变更并同步配置）。

## 数据库规范（必须）

- **审计字段**：所有表必须包含 `create_time`、`update_time`。
- **删除策略**：优先逻辑删除，增加 `deleted` 字段标识（遵循项目既有字段类型与默认值约定）。
- **命名风格**：数据库字段使用下划线；Java 实体类使用驼峰。
- **MyBatis-Plus 查询**：优先使用 `LambdaQueryWrapper` 与实体属性引用，避免硬编码字符串字段名。
- **参数化查询**：所有数据库操作必须参数化，防止 SQL 注入。
- **循环内禁 DB**：杜绝在循环中逐条读写数据库；应批量查询、内存分组处理，循环外批量写回。

## 安全规范（必须）

- **入参校验**：所有外部输入必须进行参数校验（空值、范围、枚举值/字典值、长度、格式）。
- **日志脱敏**：敏感信息禁止在日志中输出（token、身份证、手机号、密码/密钥等）。

## 性能规范（必须）

- **避免 N+1**：关联数据一次性批量查询/预加载；禁止在循环内频繁查库。
- **大数据分页**：大数据量查询必须分页处理；接口/导出需评估限流与超时。
- **缓存一致性**：引入缓存必须说明一致性策略（更新/失效/过期）。

## 日志与可观测性（必须）

- **关键操作留痕**：关键业务操作必须记录操作日志（谁、何时、做了什么、影响了什么）。
- **异常上下文**：异常日志必须包含足够上下文（关键业务 id、请求关键字段、当前用户、链路信息等）。
- **生产日志级别**：生产环境禁止输出 debug 级别日志。

## 交付前自检清单（每次改代码都要做）

- **规则自检**：是否触碰了 Controller 业务逻辑/对象转换/PathVariable/空 Mapping 等红线？
- **编译自检（必做）**：修改 Java 代码后必须重新编译，确保编译成功。
  - 推荐：只编译受影响模块（示例）
    - `mvn -q -pl :<module-artifactId> -am -DskipTests package`
  - 不确定模块时：在根目录执行
    - `mvn -q -DskipTests package`
- **最小影响**：是否只修改了需求相关代码？是否避免了无关改动？

## 违例处理（强制）

- **无法满足任意“必须/禁止”条款时**：必须先解释原因、给出替代方案，并在得到用户确认后才允许继续。

